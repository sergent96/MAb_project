Code for MAbs project at Wesemann lab
--effort to streamline process from PBD to IGBlast to allow pymol model to be labeled for analysis
--currently shows distance between different regions but that may change with future iterations

why?
-pymol command has to be manually input unless we run script, which can do the same with less work and more accuracy. 
-check to see if results have same accuracy as doing it w/o the script (but so far, it looks identical to manual code)

To operate:
-change values in list to desired data. 
--name then residue seq or new name/old name, etc. read comments for further instructions

Working
-incorporate SHM analysis to see distances between SHM and chain

